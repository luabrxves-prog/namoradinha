name: Build Windows EXE

on:
  workflow_dispatch:
  push:
    branches: ["main"]
    paths:
      - "love_system.py"
      - "app_parts/**"
      - "requirements.txt"
      - "assets/**"
      - ".github/workflows/build-windows.yml"

jobs:
  build:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: pip install -r requirements.txt pyinstaller

      - name: Gerar executavel
        run: python -m PyInstaller --noconfirm --clean --onefile --windowed --name LoveSystem --collect-all webview --add-data "app_parts;app_parts" --add-data "assets/camera.webp;assets" --add-data "assets/trio.webp;assets" --add-data "assets/dexter.webp;assets" --add-data "assets/uno.png;assets" --add-data "assets/beach.webp;assets" love_system.py

      - name: Upload EXE
        uses: actions/upload-artifact@v4
        with:
          name: LoveSystem-Windows
          path: dist/LoveSystem.exe
          if-no-files-found: error
