function render(){locked=false;attempt=0;heartLost=false;tabs();const p=phases[phase],q=p.questions[idx];document.getElementById('phaseLabel').textContent=p.label;document.getElementById('phaseLabel').className='ey '+(phase==='dexter'?'red':'');document.getElementById('phaseTitle').textContent=p.title;document.getElementById('phaseSubtitle').textContent=p.subtitle;document.getElementById('tag').textContent=q.tag||('PERGUNTA '+(idx+1));document.getElementById('counter').textContent=(idx+1)+'/'+p.questions.length;document.getElementById('progressBar').style.width=((idx+1)/p.questions.length*100)+'%';document.getElementById('question').textContent=q.q;document.getElementById('attempts').textContent='2 tentativas por pergunta';document.getElementById('feedback').className='feedback';document.getElementById('feedback').textContent='Selecione uma resposta.';document.getElementById('next').style.display='none';renderHearts();const c=document.getElementById('choices');c.innerHTML='';q.o.forEach((o,i)=>{const b=document.createElement('button');b.className='choice';b.innerHTML='<span class="letter">'+String.fromCharCode(65+i)+'</span><span>'+o+'</span>';b.onclick=()=>answer(i);c.appendChild(b)});renderSide(q)}
function renderHearts(){const h=document.getElementById('hearts');h.innerHTML='';if(phase!=='us')return;for(let i=0;i<4;i++){const s=document.createElement('span');s.className='heart'+(i<lives?'':' dead');s.textContent=i<lives?'♥':'💔';h.appendChild(s)}}
function ideHTML(kind){
 const code={
  closure:'<span class="ln">1</span><span class="kw">funcs</span> = [<span class="kw">lambda</span>: i <span class="kw">for</span> i <span class="kw">in</span> range(<span class="num">3</span>)]\n<span class="ln">2</span>print([f() <span class="kw">for</span> f <span class="kw">in</span> funcs])\n\n<span class="cm"># late binding: cuidado com closures</span>',
  sql:'<span class="ln">1</span><span class="kw">WITH</span> ranked <span class="kw">AS</span> (\n<span class="ln">2</span>  <span class="kw">SELECT</span> *,\n<span class="ln">3</span>    ROW_NUMBER() <span class="kw">OVER</span> (\n<span class="ln">4</span>      PARTITION BY cliente_id\n<span class="ln">5</span>      ORDER BY data DESC\n<span class="ln">6</span>    ) rn\n<span class="ln">7</span>  <span class="kw">FROM</span> pedidos\n<span class="ln">8</span>)',
  pipeline:'<span class="ln">1</span><span class="fn">def</span> ingest(batch):\n<span class="ln">2</span>    key = stable_key(batch)\n<span class="ln">3</span>    warehouse.<span class="fn">merge</span>(key, batch)\n<span class="ln">4</span>    checkpoint.<span class="fn">commit</span>(batch.id)\n\n<span class="cm"># rerun safe / idempotent</span>',
  git:'<span class="ln">$</span> git log --oneline\n<span class="ln">a91f2c</span> bad commit\n<span class="ln">82d40b</span> previous state\n\n<span class="ln">$</span> git revert a91f2c\n<span class="str">[main 1b4c91] Revert "bad commit"</span>',
  mutability:'<span class="ln">1</span>x = [1, 2, 3]\n<span class="ln">2</span>y = x\n<span class="ln">3</span>x += [4]\n<span class="ln">4</span>print(x is y, y)\n\n<span class="cm"># identidade ≠ cópia</span>',
  deterministic:'<span class="ln">1</span>ROW_NUMBER() OVER (\n<span class="ln">2</span>  PARTITION BY cliente_id\n<span class="ln">3</span>  ORDER BY data DESC, id_pedido DESC\n<span class="ln">4</span>) AS rn\n\n<span class="cm"># empate resolvido de forma estável</span>'
 }[kind]||'';
 return '<div class="ide"><div class="activity"><b>◫</b><span>⌕</span><span>⑂</span><span>▷</span><span>⚙</span></div><div class="explorer"><strong>EXPLORADOR</strong><br>▾ love_system<br>&nbsp;&nbsp;▾ knowledge<br><span class="sel">&nbsp;&nbsp;&nbsp;programacao</span>&nbsp;&nbsp;algoritmos<br>&nbsp;&nbsp;dados<br>&nbsp;&nbsp;tests<br>&nbsp;&nbsp;assets<br>&nbsp;&nbsp;.gitignore<br>&nbsp;&nbsp;README.md</div><div class="idetabs"><span>● main.py &nbsp; ×</span></div><div class="editor"><pre>'+code+'</pre></div><div class="terminalPane"><div class="tabsline">TERMINAL &nbsp;&nbsp; PROBLEMAS &nbsp;&nbsp; SAÍDA &nbsp;&nbsp; DEBUG CONSOLE</div><span class="str">(love) ~/love_system/knowledge/programacao</span><br>$ python main.py<br><span class="str">Processando...</span><br>Tudo certo! ♡<br>Process finished with exit code 0</div></div><div class="ideScrib">good code<br>better us<br>♡</div>'
}
function renderSide(q){const s=document.getElementById('side');if(phase==='programming'){s.className='sidevisual';s.innerHTML=ideHTML(q.visual);return}if(phase==='dexter'&&q.photo){s.className='trioVisual';s.innerHTML='<img src="assets/trio.webp" alt="Nossa foto"><div class="scrib">nós três<br>num crossover impossível ♡</div><div class="caption">último arquivo da fase Dexter • recompensa desbloqueada</div>';return}if(phase==='dexter'){s.className='dexterVisual';s.innerHTML='<img src="assets/dexter.webp" alt="Dexter"><div class="dexnote">SOME PEOPLE JUST NEED A CLEANER WORLD.</div>';return}s.className='romanticPanel';s.innerHTML='<div><div class="bigHeart">♡</div><h2>pequenas coisas,<br>grandes sentidos.</h2><p>Mesmo sistema. Mais amor. Sempre.</p></div><div class="log">[OK] memória carregada<br>[OK] você continua sendo minha pessoa favorita<br>[OK] futuro: nós<br>[OK] carinho: sem limite</div>'}
